import pytest
from sample import check_prime_number


@pytest.mark.parametrize(
    ('number', 'expected'),
    [
        pytest.param(1, False, id="test1"),    # 1は素数ではない
        pytest.param(2, True, id="test2"),     # 2は素数
        pytest.param(3, True, id="test3"),     # 3は素数
        pytest.param(4, False, id="test4"),    # 4は素数ではない
        pytest.param(5, True, id="test5"),     # 5は素数
        pytest.param(6, False, id="test6"),    # 6は素数ではない
        pytest.param(7, True, id="test7"),     # 7は素数
        pytest.param(8, False, id="test8"),    # 8は素数ではない
        pytest.param(9, False, id="test9"),    # 9は素数ではない
        pytest.param(10, False, id="test10"),  # 10は素数ではない

        pytest.param(11, True, id="test11"),   # 11は素数
        pytest.param(12, False, id="test12"),  # 12は素数ではない
        pytest.param(13, True, id="test13"),   # 13は素数
        pytest.param(14, False, id="test14"),  # 14は素数ではない
        pytest.param(15, False, id="test15"),  # 15は素数ではない
        pytest.param(16, False, id="test16"),  # 16は素数ではない
        pytest.param(17, True, id="test17"),   # 17は素数
        pytest.param(18, False, id="test18"),  # 18は素数ではない
        pytest.param(19, True, id="test19"),   # 19は素数
        pytest.param(20, False, id="test20"),  # 20は素数ではない
        pytest.param(23, True, id="test23"),   # 23は素数
        pytest.param(29, True, id="test29"),   # 29は素数
        pytest.param(31, True, id="test31"),   # 31は素数
        pytest.param(37, True, id="test37"),   # 37は素数
        pytest.param(41, True, id="test41"),   # 41は素数
        pytest.param(43, True, id="test43"),   # 43は素数
        pytest.param(47, True, id="test47"),   # 47は素数
        pytest.param(53, True, id="test53"),   # 53は素数
        pytest.param(59, True, id="test59"),   # 59は素数
        pytest.param(61, True, id="test61"),   # 61は素数
        pytest.param(67, True, id="test67"),   # 67は素数
        pytest.param(71, True, id="test71"),   # 71は素数
        pytest.param(73, True, id="test73"),   # 73は素数
        pytest.param(79, True, id="test79"),   # 79は素数
        pytest.param(83, True, id="test83"),   # 83は素数
        pytest.param(89, True, id="test89"),   # 89は素数
        pytest.param(97, True, id="test97"),   # 97は素数

        pytest.param(21, False, id="test21"),  # 21は素数ではない
        pytest.param(25, False, id="test25"),  # 25は素数ではない
        pytest.param(27, False, id="test27"),  # 27は素数ではない
        pytest.param(33, False, id="test33"),  # 33は素数ではない
        pytest.param(35, False, id="test35"),  # 35は素数ではない
        pytest.param(39, False, id="test39"),  # 39は素数ではない
        pytest.param(45, False, id="test45"),  # 45は素数ではない
        pytest.param(49, False, id="test49"),  # 49は素数ではない
        pytest.param(51, False, id="test51"),  # 51は素数ではない
        pytest.param(55, False, id="test55"),  # 55は素数ではない
        pytest.param(57, False, id="test57"),  # 57は素数ではない
        pytest.param(63, False, id="test63"),  # 63は素数ではない
        pytest.param(65, False, id="test65"),  # 65は素数ではない
        pytest.param(69, False, id="test69"),  # 69は素数ではない
        pytest.param(75, False, id="test75"),  # 75は素数ではない
        pytest.param(81, False, id="test81"),  # 81は素数ではない
        pytest.param(85, False, id="test85"),  # 85は素数ではない
        pytest.param(91, False, id="test91"),  # 91は素数ではない
        pytest.param(93, False, id="test93"),  # 93は素数ではない
        pytest.param(95, False, id="test95"),  # 95は素数ではない
        pytest.param(99, False, id="test99"),  # 99は素数ではない
        pytest.param(101, True, id="test101"),   # 101は素数
        pytest.param(103, True, id="test103"),   # 103は素数
        pytest.param(107, True, id="test107"),   # 107は素数
        pytest.param(109, True, id="test109"),   # 109は素数
        pytest.param(113, True, id="test113"),   # 113は素数
        pytest.param(127, True, id="test127"),   # 127は素数
        pytest.param(131, True, id="test131"),   # 131は素数
        pytest.param(137, True, id="test137"),   # 137は素数
        pytest.param(149, True, id="test149"),   # 149は素数
        pytest.param(151, True, id="test151"),   # 151は素数
        pytest.param(157, True, id="test157"),   # 157は素数
        pytest.param(163, True, id="test163"),   # 163は素数
        pytest.param(167, True, id="test167"),   # 167は素数
        pytest.param(173, True, id="test173"),   # 173は素数
        pytest.param(179, True, id="test179"),   # 179は素数
        pytest.param(181, True, id="test181"),   # 181は素数
        pytest.param(191, True, id="test191"),   # 191は素数
        pytest.param(193, True, id="test193"),   # 193は素数
        pytest.param(197, True, id="test197"),   # 197は素数
        pytest.param(199, True, id="test199"),   # 199は素数
        pytest.param(211, True, id="test211"),   # 211は素数
        pytest.param(223, True, id="test223"),   # 223は素数
        pytest.param(227, True, id="test227"),   # 227は素数
        pytest.param(229, True, id="test229"),   # 229は素数
        pytest.param(233, True, id="test233"),   # 233は素数
        pytest.param(239, True, id="test239"),   # 239は素数
        pytest.param(241, True, id="test241"),   # 241は素数
        pytest.param(251, True, id="test251"),   # 251は素数
        pytest.param(257, True, id="test257"),   # 257は素数
        pytest.param(263, True, id="test263"),   # 263は素数
        pytest.param(269, True, id="test269"),   # 269は素数
        pytest.param(271, True, id="test271"),   # 271は素数
        pytest.param(277, True, id="test277"),   # 277は素数
        pytest.param(281, True, id="test281"),   # 281は素数
        pytest.param(283, True, id="test283"),   # 283は素数
        pytest.param(293, True, id="test293"),   # 293は素数
        pytest.param(307, True, id="test307"),   # 307は素数
        pytest.param(311, True, id="test311"),   # 311は素数
        pytest.param(313, True, id="test313"),   # 313は素数
        pytest.param(317, True, id="test317"),   # 317は素数
        pytest.param(331, True, id="test331"),   # 331は素数
        pytest.param(337, True, id="test337"),   # 337は素数
        pytest.param(347, True, id="test347"),   # 347は素数
        pytest.param(349, True, id="test349"),   # 349は素数
        pytest.param(353, True, id="test353"),   # 353は素数
        pytest.param(359, True, id="test359"),   # 359は素数
        pytest.param(367, True, id="test367"),   # 367は素数
        pytest.param(373, True, id="test373"),   # 373は素数
        pytest.param(379, True, id="test379"),   # 379は素数
        pytest.param(383, True, id="test383"),   # 383は素数
        pytest.param(389, True, id="test389"),   # 389は素数
        pytest.param(397, True, id="test397"),   # 397は素数
        pytest.param(401, True, id="test401"),   # 401は素数
        pytest.param(409, True, id="test409"),   # 409は素数
        pytest.param(419, True, id="test419"),   # 419は素数
        pytest.param(421, True, id="test421"),   # 421は素数
        pytest.param(431, True, id="test431"),   # 431は素数
        pytest.param(433, True, id="test433"),   # 433は素数
        pytest.param(439, True, id="test439"),   # 439は素数
        pytest.param(443, True, id="test443"),   # 443は素数
        pytest.param(449, True, id="test449"),   # 449は素数
        pytest.param(457, True, id="test457"),   # 457は素数
        pytest.param(461, True, id="test461"),   # 461は素数
        pytest.param(463, True, id="test463"),   # 463は素数
        pytest.param(467, True, id="test467"),   # 467は素数
        pytest.param(479, True, id="test479"),   # 479は素数
        pytest.param(487, True, id="test487"),   # 487は素数
        pytest.param(491, True, id="test491"),   # 491は素数
        pytest.param(499, True, id="test499"),   # 499は素数
        pytest.param(121, False, id="test121"),  # 121は素数ではない
        pytest.param(143, False, id="test143"),  # 143は素数ではない
        pytest.param(169, False, id="test169"),  # 169は素数ではない
        pytest.param(201, False, id="test201"),  # 201は素数ではない
        pytest.param(217, False, id="test217"),  # 217は素数ではない
        pytest.param(221, False, id="test221"),  # 221は素数ではない
        pytest.param(247, False, id="test247"),  # 247は素数ではない
        pytest.param(253, False, id="test253"),  # 253は素数ではない
        pytest.param(289, False, id="test289"),  # 289は素数ではない
        pytest.param(299, False, id="test299"),  # 299は素数ではない
        pytest.param(323, False, id="test323"),  # 323は素数ではない
        pytest.param(341, False, id="test341"),  # 341は素数ではない
        pytest.param(371, False, id="test371"),  # 371は素数ではない
        pytest.param(391, False, id="test391"),  # 391は素数ではない
        pytest.param(399, False, id="test399"),  # 399は素数ではない
        pytest.param(413, False, id="test413"),  # 413は素数ではない
        pytest.param(427, False, id="test427"),  # 427は素数ではない
        pytest.param(451, False, id="test451"),  # 451は素数ではない
        pytest.param(469, False, id="test469"),  # 469は素数ではない
        pytest.param(473, False, id="test473"),  # 473は素数ではない
        pytest.param(481, False, id="test481"),  # 481は素数ではない
        pytest.param(503, True, id="test503"),   # 503は素数
        pytest.param(509, True, id="test509"),   # 509は素数
        pytest.param(521, True, id="test521"),   # 521は素数
        pytest.param(523, True, id="test523"),   # 523は素数
        pytest.param(541, True, id="test541"),   # 541は素数
        pytest.param(547, True, id="test547"),   # 547は素数
        pytest.param(557, True, id="test557"),   # 557は素数
        pytest.param(563, True, id="test563"),   # 563は素数
        pytest.param(569, True, id="test569"),   # 569は素数
        pytest.param(571, True, id="test571"),   # 571は素数
        pytest.param(577, True, id="test577"),   # 577は素数
        pytest.param(587, True, id="test587"),   # 587は素数
        pytest.param(593, True, id="test593"),   # 593は素数
        pytest.param(599, True, id="test599"),   # 599は素数
        pytest.param(601, True, id="test601"),   # 601は素数
        pytest.param(607, True, id="test607"),   # 607は素数
        pytest.param(613, True, id="test613"),   # 613は素数
        pytest.param(617, True, id="test617"),   # 617は素数
        pytest.param(619, True, id="test619"),   # 619は素数
        pytest.param(631, True, id="test631"),   # 631は素数
        pytest.param(641, True, id="test641"),   # 641は素数
        pytest.param(643, True, id="test643"),   # 643は素数
        pytest.param(647, True, id="test647"),   # 647は素数
        pytest.param(653, True, id="test653"),   # 653は素数
        pytest.param(659, True, id="test659"),   # 659は素数
        pytest.param(661, True, id="test661"),   # 661は素数
        pytest.param(673, True, id="test673"),   # 673は素数
        pytest.param(677, True, id="test677"),   # 677は素数
        pytest.param(683, True, id="test683"),   # 683は素数
        pytest.param(691, True, id="test691"),   # 691は素数
        pytest.param(701, True, id="test701"),   # 701は素数
        pytest.param(709, True, id="test709"),   # 709は素数
        pytest.param(719, True, id="test719"),   # 719は素数
        pytest.param(727, True, id="test727"),   # 727は素数
        pytest.param(733, True, id="test733"),   # 733は素数
        pytest.param(739, True, id="test739"),   # 739は素数
        pytest.param(743, True, id="test743"),   # 743は素数
        pytest.param(751, True, id="test751"),   # 751は素数
        pytest.param(757, True, id="test757"),   # 757は素数
        pytest.param(761, True, id="test761"),   # 761は素数
        pytest.param(769, True, id="test769"),   # 769は素数
        pytest.param(773, True, id="test773"),   # 773は素数
        pytest.param(787, True, id="test787"),   # 787は素数
        pytest.param(797, True, id="test797"),   # 797は素数
        pytest.param(809, True, id="test809"),   # 809は素数
        pytest.param(811, True, id="test811"),   # 811は素数
        pytest.param(821, True, id="test821"),   # 821は素数
        pytest.param(823, True, id="test823"),   # 823は素数
        pytest.param(827, True, id="test827"),   # 827は素数
        pytest.param(829, True, id="test829"),   # 829は素数
        pytest.param(839, True, id="test839"),   # 839は素数
        pytest.param(853, True, id="test853"),   # 853は素数
        pytest.param(857, True, id="test857"),   # 857は素数
        pytest.param(859, True, id="test859"),   # 859は素数
        pytest.param(863, True, id="test863"),   # 863は素数
        pytest.param(877, True, id="test877"),   # 877は素数
        pytest.param(881, True, id="test881"),   # 881は素数
        pytest.param(883, True, id="test883"),   # 883は素数
        pytest.param(887, True, id="test887"),   # 887は素数
        pytest.param(907, True, id="test907"),   # 907は素数
        pytest.param(911, True, id="test911"),   # 911は素数
        pytest.param(919, True, id="test919"),   # 919は素数
        pytest.param(929, True, id="test929"),   # 929は素数
        pytest.param(937, True, id="test937"),   # 937は素数
        pytest.param(941, True, id="test941"),   # 941は素数
        pytest.param(947, True, id="test947"),   # 947は素数
        pytest.param(953, True, id="test953"),   # 953は素数
        pytest.param(967, True, id="test967"),   # 967は素数
        pytest.param(971, True, id="test971"),   # 971は素数
        pytest.param(977, True, id="test977"),   # 977は素数
        pytest.param(983, True, id="test983"),   # 983は素数
        pytest.param(991, True, id="test991"),   # 991は素数
        pytest.param(997, True, id="test997"),   # 997は素数

        pytest.param(511, False, id="test511"),  # 511は素数ではない
        pytest.param(527, False, id="test527"),  # 527は素数ではない
        pytest.param(539, False, id="test539"),  # 539は素数ではない
        pytest.param(551, False, id="test551"),  # 551は素数ではない
        pytest.param(561, False, id="test561"),  # 561は素数ではない
        pytest.param(573, False, id="test573"),  # 573は素数ではない
        pytest.param(581, False, id="test581"),  # 581は素数ではない
        pytest.param(589, False, id="test589"),  # 589は素数ではない
        pytest.param(597, False, id="test597"),  # 597は素数ではない
        pytest.param(603, False, id="test603"),  # 603は素数ではない
        pytest.param(609, False, id="test609"),  # 609は素数ではない
        pytest.param(611, False, id="test611"),  # 611は素数ではない
        pytest.param(621, False, id="test621"),  # 621は素数ではない
        pytest.param(633, False, id="test633"),  # 633は素数ではない
        pytest.param(637, False, id="test637"),  # 637は素数ではない
        pytest.param(649, False, id="test649"),  # 649は素数ではない
        pytest.param(657, False, id="test657"),  # 657は素数ではない
        pytest.param(667, False, id="test667"),  # 667は素数ではない
        pytest.param(679, False, id="test679"),  # 679は素数ではない
        pytest.param(681, False, id="test681"),  # 681は素数ではない
        pytest.param(687, False, id="test687"),  # 687は素数ではない

        pytest.param(997, True, id="test997"),   # 997は素数
        pytest.param(998, False, id="test998"),  # 998は素数ではない
        pytest.param(999, False, id="test999"),  # 999は素数ではない
        pytest.param(1000, False, id="test1000"),  # 1000は素数ではない
    ]
)
def test_is_prime(number, expected):
    assert check_prime_number(number) == expected
